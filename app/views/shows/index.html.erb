<ul>
<h3><%= link_to "View all your characters", user_show_characters_path(current_user, current_user.shows) %></h3>
<br>
<h2><%= link_to "Add new show", new_user_show_path %></h2>

<h1>Your shows:</h1>

<% current_user.shows.each do |show| %>
  <h3> <%= link_to show.name, user_show_path(show.user_id, show.id) %> </h3>
<% end %>

</ul>
