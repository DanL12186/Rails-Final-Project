<h3>
<center>

<%= link_to "Home", root_url if logged_in? %>
<%= link_to "User Profile", user_path(current_user) if logged_in?%>

<%= link_to "Sign up", new_user_path unless logged_in? %>
<%= link_to "Log in", login_path unless logged_in? %>
<%= link_to "Sign in with Facebook", "/auth/facebook" unless logged_in? %>
<%= link_to "Log Out", logout_path, method: :delete if logged_in? %>

<%= link_to "Edit Profile", edit_user_path(current_user) if logged_in? %>
<br>
<%= link_to "View Your Shows", user_shows_path(current_user) if logged_in? && !current_user.shows.empty? %>

</center>
</h3>
